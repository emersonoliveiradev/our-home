{{ form_start(edit_form, { attr: { novalidate: 'novalidate', class: form_css|default('') } }) }}
    {% if edit_form.vars.method == "DELETE" %}

    {% else %}
        <div class="row" style="margin-top: 10px">
            <div class="col-xs-12 {% if edit_form.title.vars.errors|length %} has-error {% endif %}">
                <div class="form-group">
                        {{ form_label(edit_form.title) }}
                        {{ form_widget(edit_form.title) }}
                        {{ form_errors(edit_form.title) }}
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-xs-12 {% if edit_form.description.vars.errors|length %} has-error {% endif %}">
                <div class="form-group">
                    {{ form_label(edit_form.description) }}
                    {{ form_widget(edit_form.description) }}
                    {{ form_errors(edit_form.description) }}
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-xs-12 {% if edit_form.surrounding.vars.errors|length %} has-error {% endif %}">
                <div class="form-group">
                    {{ form_label(edit_form.surrounding) }}
                    {{ form_widget(edit_form.surrounding) }}
                    {{ form_errors(edit_form.surrounding) }}
                </div>
            </div>
            <div class="col-xs-4 {% if edit_form.status.vars.errors|length %} has-error {% endif %}">
                <div class="form-group">
                    <div col-xs-12>
                    {{ form_label(edit_form.status) }}
                    </div>
                    <div col-xs-12>
                    {{ form_widget(edit_form.status) }}
                    </div>
                    <div col-xs-12>
                    {{ form_errors(edit_form.status) }}
                    </div>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-xs-offset-7" {% if edit_form.description.vars.errors|length %} has-error {% endif %}">
                <div class="form-actions" style="float: left">
                    <a href="{{ path('component_index') }}">
                        <span class="btn btn-warning waves-effect">
                            Voltar
                        </span>
                    </a>
                </div>
                <div class="form-actions" style="float: left; margin-left: 5px">
                    <button type="submit" class="btn btn-success waves-effect">
                        {{ button_label|default('Salvar') }}
                    </button>
                </div>
            </div>
        </div>
    {% endif %}


{{ form_end(edit_form) }}